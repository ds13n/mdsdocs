# Command: Deploy

## Description
This command documents the deployment process for the `mdsdocs` project.

## Workflow
1. Run `mint broken-links` to ensure all links are valid.
2. Ensure all changes are committed and pushed to `main`.
3. The site is automatically deployed via CI/CD on Push.

## Requirements
- `mintlify` CLI installed.
- Access to the target environment.
